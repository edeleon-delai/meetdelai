node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
.vercel

# TypeScript incremental build cache — regenerated by every `tsc -b`,
# so tracking it churns the diff on every commit and conflicts on every merge.
*.tsbuildinfo
